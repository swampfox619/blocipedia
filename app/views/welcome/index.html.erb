

<div class="row">
<div class="col-sm-offset-2 col-sm-8" style="margin-bottom: 50px;">
    <h2>Blocipedia: Social, Markdown Wikis</h1>
</div>
</div>
<div class="row">
    <div class="col-sm-offset-2 col-sm-8" style="margin: 50px; border: 1px solid black; padding: 50px; text-align:center;">
        <h3>Create and Share Markdown Wikis</h3>
        <% if current_user  %>
            <%= "Welcome, #{current_user.email}!"%>
        <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
        <% end %>
    </div>
</div>

<div class="row">
    <% if current_user %>
        <div class="col-sm-offset-1 col-sm-6" style="height: 150px; text-align: center;">
            <h4 style=>Create private wikis!</h4>
            <a class="btn btn-primary" href="#" role="button">Upgrade Now!</a>
        </div>
    <% else %>
        <div class="col-sm-4" style="height: 150px; text-align: center;">
            <h4>Unlimited Public Wikis!</h4>
            <%= link_to "Free Plan", new_user_registration_path, class: "btn btn-primary" %>
        </div>
        <div class="col-sm-4" style="height: 150px; text-align: center;">
            <h4 style=>Create private wikis!</h4>
            <a class="btn btn-primary" href="#" role="button">Premium Plan</a>
        </div>
    <% end %>
</div>
